- name: install updates
  hosts: ubuntu
  tasks:
          - name: Updating host using apt
            apt:
                    update_cache: yes
                    upgrade: dist
          - name: Clean unwanted olderstuff
            apt:
                    autoremove: yes
                    purge: yes
